{
  "$schema": "https://developer.microsoft.com/json-schemas/fabric/item/dataAgent/definition/dataSource/1.0.0/schema.json",
  "artifactId": "{{ .KQL_DATABASE_ID }}",
  "workspaceId": "{{ .WORKSPACE_ID }}",
  "displayName": "{{ .KQL_DATABASE_NAME }}",
  "type": "kusto",
  "userDescription": "Factory IQ Eventhouse — lossless Bronze ingestion with optional customer-defined Silver tables.",
  "dataSourceInstructions": "RawTelemetry is the stable Bronze table. Payload contains the complete source event as dynamic JSON. Use IngestedAt for ingestion-window analysis, EventTime for source-event chronology, and SourceSystem/SourceSchema/SourceRecordId for origin and traceability. Customer-defined Silver tables may exist when a routing profile is enabled; discover and use them only according to their deployed schema. Do not infer ISA-95 meanings from arbitrary Payload fields without explicit source context.",
  "elements": [
    {
      "display_name": "RawTelemetry",
      "type": "kusto.table",
      "is_selected": true,
      "description": "Lossless Bronze ingestion envelope containing complete source events"
    }
  ]
}
